<!DOCTYPE html>
<html lang="en">
<head>
    <%- include asset/lib %>
</head>
<body class="container"> 
<!-- style="width:100%; heigh:100%; padding: 0;" -->
    <%- include navbar %>
      
    <div class="container-fluid text-center">    
        <div class="row content">
        <div class="alert alert-dismissible alert-success">
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <strong>Well done!</strong> You successfully import the quiz : <%= quizName %>.
        </div>
            Quiz Name : <%= quizName %>
            <table class="table table-striped">
                <thead>
                    <th>Question</th>
                    <th>Answer</th>
                    <th>choice A</th>
                    <th>choice B</th>
                    <th>choice C</th>
                    <th>choice D</th>
                </thead>
                <tbody>
                    <% for(var i=0; i<Quiz.length; i++) {%>
                       <tr>
                            <td><%= Quiz[i].question %></td>
                            <td><%= Quiz[i].answer %></td>
                            <td><%= Quiz[i].choice.A %></td>
                            <td><%= Quiz[i].choice.B %></td>
                            <td><%= Quiz[i].choice.C %></td>
                            <td><%= Quiz[i].choice.D %></td>
                       </tr>
                    <% } %>
                </tbody>
            </table>
      </div>
    </div>

</body>
</html>